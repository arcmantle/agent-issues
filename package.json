{
	"name": "agent-issues-workspace",
	"version": "0.1.0",
	"description": "Workspace root for agent-issues: shared core, CLI, site, and deployable API.",
	"private": true,
	"type": "module",
	"engines": {
		"node": ">=24"
	},
	"scripts": {
		"build": "pnpm --filter @agent-issues/core build && pnpm --filter @agent-issues/site build && pnpm --filter agent-issues build && pnpm --filter @agent-issues/api build",
		"typecheck": "pnpm --filter @agent-issues/core build && pnpm -r typecheck",
		"test": "pnpm -r test",
		"db:generate": "pnpm --filter @agent-issues/core db:generate",
		"site:dev": "pnpm --filter @agent-issues/site dev"
	}
}
