{
	"name": "@agent-issues/api-local",
	"version": "0.1.0",
	"private": true,
	"description": "Local SQLite storage engine and daemon JSON-RPC server (the local mirror of @agent-issues/api-pg's cloud Postgres service).",
	"type": "module",
	"main": "./dist/index.js",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"engines": {
		"node": ">=24"
	},
	"scripts": {
		"build": "node --eval \"import { rmSync } from 'node:fs'; rmSync('dist', { recursive: true, force: true });\" && tsc -p tsconfig.json",
		"postbuild": "node --eval \"import { writeBuildInfoFile } from './dist/daemon/build-info.js'; writeBuildInfoFile('dist');\"",
		"benchmark:search": "pnpm --filter @agent-issues/core build && pnpm run build && node ./scripts/benchmark-search.mjs",
		"test": "vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit -p tsconfig.json"
	},
	"devDependencies": {
		"@types/better-sqlite3": "catalog:",
		"@types/node": "catalog:",
		"@types/supertest": "catalog:",
		"supertest": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	},
	"dependencies": {
		"@agent-issues/core": "workspace:*",
		"better-sqlite3": "catalog:",
		"drizzle-orm": "catalog:",
		"marked": "catalog:"
	}
}